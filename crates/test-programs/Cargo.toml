[package]
name = "test-programs"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
wasi-http-client = { path = "../../", features = ["json"] }
serde = { version = "1.0.201", features = ["derive"] }
